apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "csharp-service.fullname" . }}-test-connection"
  labels:
    {{- include "csharp-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "csharp-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
