apiVersion: v1
kind: Service
metadata:
  name: {{ include "csharp-service.fullname" . }}
  labels:
    {{- include "csharp-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "csharp-service.selectorLabels" . | nindent 4 }}
